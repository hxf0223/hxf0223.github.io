---
layout: post
title: CUDA 笔记集合
date: 2025-02-23 +0730 # 2022-01-01 13:14:15 +0800 只写日期也行；不写秒也行；这样也行 2022-03-09T00:55:42+08:00
categories: [HPC]
tags: [CUDA, CuTe]

# 以下默认false
math: true
mermaid: true
# pin: true

toc:
  sidebar: right
---

## 1. cutlass/CuTe GEMM 中矩阵的存储方式 NT / TN / NN / TT

### 1.1. 背景

BLAS 的约定是：所有矩阵一律按 column-major 存储，然后用 transA/transB 标志告诉 BLAS 要不要对它做转置：

$$C = \alpha \cdot op(A) \cdot op(B) + \beta \cdot C$$

其中：

- 当`transX`为`N`时：$op(X) = X$，当`transX`为`T`时：$op(X) = X^T$。
- 乘法要求 $op(A)$ 是 $M \times K$，$op(B)$ 是 $K \times N$。

### 1.2. GEMM 命名含义

`CuTe`给矩阵做了一个约定：A(M, K)，B(N, K)，C(M, N)，即：

- A 矩阵：(M，K) -- M 行 K 列
- B 矩阵：(N，K) -- N 行 K 列（不同于 BLAS 及其他典型约定）
- C 矩阵：(M，N) -- M 行 N 列

> 即 CuTe 对 B 的约定，正好与 BLAS 约定形成转置关系。

**gemm_nt**

```cpp
template <class TA, class TB, class TC, class Alpha, class Beta>
static cudaError_t gemm_nt(int m, int n, int k, Alpha alpha, TA const* A,
                           int ldA, TB const* B, int ldB, Beta beta, TC* C,
                           int ldC, cudaStream_t stream)
```

`nt`含义是：

A 是 (M, K)，column-major。由于 BLAS 的约定，$B^T$ 是 (K, N)，因此 B 本身是 (N, K)，column-major，正好与 CuTe 约定的 B 矩阵形状一致，且是 column-major。因此得到：

```cpp
strider_A = cute::make_stride(cute::_1, ldA); // column-major
shape_A = cute::make_shape(M, K);

stride_B = cute::make_stride(cute::_1, ldB); // column-major
shape_B = cute::make_shape(N, K);

stride_C = cute::make_stride(cute::_1, ldC); // column-major
shape_C = cute::make_shape(M, N);
```

**gemm_tn**

- $A^T$是(M, K)，所以此时 A 是 (K, M)，column-major；按照 CuTe 约定，要存储 $A^T$，只能是 (M, K) + row-major。
- 类似的道理，由于 CuTe 对 B 的约定导致其存储格式与 BLAS 形成转置关系。